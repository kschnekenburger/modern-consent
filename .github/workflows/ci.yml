name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20, 22]
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Format check
        run: pnpm format:check

      - name: Typecheck
        run: pnpm typecheck

      - name: Test
        run: pnpm test

      - name: Build
        run: pnpm build

      - name: Check bundle sizes
        run: |
          echo "📦 Core bundle:"
          du -sh packages/core/dist/index.js
          echo "📦 Widget CDN bundle:"
          du -sh packages/widget/dist/mc-widget.js
          echo "📦 Vendor files:"
          ls -lh packages/vendors/dist/*.js | grep -v chunk | grep -v map
